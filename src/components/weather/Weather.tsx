import React from "react";
import CityCard from "./CityCard";

const WeatherApp = () => {
  return (
    <div>
      <h2>Weather App</h2>
      <CityCard city="Chicago" />
    </div>
  );
};

export default WeatherApp;
